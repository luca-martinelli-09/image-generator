export default function Settings() {
  return (
    <div className="p-4 border rounded-lg">
      <p className="text-sm text-gray-600">
        API Key and model are configured server-side via environment variables.
      </p>
    </div>
  );
}
